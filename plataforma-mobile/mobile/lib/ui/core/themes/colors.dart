//define a pallete de cores usada na app como constantes

import 'package:flutter/widgets.dart';

class AppColors {
  static const primary = Color(0xFF3C5D8E);
  static const secondary = Color(0xFF00B2E3);
  static const background = Color(0xFFFFFFFF);
}